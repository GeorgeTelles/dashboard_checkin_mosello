🔧 CORREÇÃO DO DOCKERFILE
═══════════════════════════════════════════════════════════════════════════════

O erro foi:
  "npm ci --only=production && npm ci --save-dev @types/node..."

Problema:
  • npm ci --only=production não instala dev dependencies
  • Depois npm ci --save-dev tenta instalar, mas não funciona assim
  • O comando estava malformado

Solução:
  • No estágio BUILD: npm install (instala tudo)
  • No estágio PRODUCTION: npm ci --only=production (só prod)

═══════════════════════════════════════════════════════════════════════════════

✅ DOCKERFILE CORRIGIDO

Stage 1 (BUILD):
  - npm install (instala todas as dependencies)
  - npm run build (compila Vite)

Stage 2 (PRODUCTION):
  - npm ci --only=production (apenas prod)
  - Copia frontend buildado
  - Roda Express

═══════════════════════════════════════════════════════════════════════════════

🚀 PRÓXIMOS PASSOS

1. Fazer git push novamente
2. No Portainer: Delete stack anterior
3. Add Stack novamente
4. Deploy

═══════════════════════════════════════════════════════════════════════════════

git add .
git commit -m "Fix Dockerfile npm install"
git push origin main

═══════════════════════════════════════════════════════════════════════════════
